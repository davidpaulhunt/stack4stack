= form_for @jobseeker do |f|
  - if @jobseeker.errors.any?
    #error_explanation
      %h2= "#{pluralize(@jobseeker.errors.count, "error")} prohibited this jobseeker from being saved:"
      %ul
        - @jobseeker.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
